import { CustomConfirmValidator } from './custom-confirm-validator';

describe('CustomConfirmValidator', () => {
  it('should create an instance', () => {
    expect(new CustomConfirmValidator()).toBeTruthy();
  });
});
